# 改善実施・検証記録

2026-09-08 / P-9 / T-88・T-89

## 実施済み

- 本番 https://tokuso-serenshia.com/ に料金要約（既存事例15万・20万・33万円）をFV直後へ追加。詳細リンクを接続。
- LINE相談は写真必須ではないと明記。大家・管理会社向けに立会い不要・写真報告等の既存対応を整理。
- 電話営業時間判定を日本時間に固定。FAQ本文と構造化データの案内を整合。
- 競合比較・試算・90日計画・実施状況のHTMLを作成。画像埋込版は `output/marketing/improvement-plan.html`。印刷PDFも同ディレクトリに保存。
- 広告案はPaused指定のCSV3案、成約台帳はヘッダーのみ。広告配信済みや架空実績ではない。

## 検証証跡

- `npm run build` 成功。型チェック、8ページ生成、静的export成功。`git diff --check` 合格。
- REV-9 / ag-306による独立レビュー・ブラウザQA合格。初回の料金アンカー不一致を修正し再検証。
- LP 320/390/1440pxで横はみ出しなし。料金詳細5リンクの実在確認、CTAイベントは操作ごとに1回。電話番号維持。
- HTML初期必要費用1,031,250円、問い合わせ率6%で687,500円。空・0・101%の不正入力を拒否。予算0時は成約0を表示。印刷検証合格。
- 公開後HTTP 200。390/1440pxで画像欠落0、横はみ出しなし、新規2セクションを確認。画像証跡は `output/playwright/live-390.png` と `live-1440.png`。
- CTA検証はブラウザ内gtag呼び出しの検証であり、Google広告への成約インポート完了を意味しない。

## 公開・保全

- 作業ブランチ: `seat/serenshi-ag-301-mtrczktl`。実装 `6050cb5`、バックアップ保存先修正 `dd8d0bb` をpush済み。mainへのマージは未実施。
- 公開前バックアップ: SSHユーザーの `$HOME/serenshia-backups/serenshia-20260908020901.tgz`。公開外・権限制限付き。
- 初回の `/var/www` 直下バックアップは権限拒否で中止し、その時点では本番変更なし。保存先修正後にバックアップ取得・配信成功。
- 静的 `out/` のみ配信し、内部計画書・CSV・認証情報は配信しない。削除オプションは使わず既存資産を保持。

## 未実施・継続事項

- Google Ads接続は `invalid_grant` を再現。広告の設定・予算・配信は未変更。再認証後に最新実績と既存設定を確認してから入稿判断する。
- 月30成約・利益改善は未検証。HTMLの数値は条件計算であり達成予測ではない。
- 成約台帳の実運用と90日間の効果検証は今後の運用。成約・取消・原価が蓄積される前の増額は行っていない。
